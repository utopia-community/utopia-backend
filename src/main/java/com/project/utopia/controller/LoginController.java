package com.project.utopia.controller;

public class LoginController {
    // to be determined if this part is needed
    // if Spring Security features can cover log in, logout function, evaluate if LoginController is still needed
}
